pipeline { 

    agent any 

    stages { stage("build") { steps { echo 'main branch-building environment...' } }
             stage("test") { steps { echo 'main branch-testing new fitchers...' }  } 
             stage("deploy") { steps { echo 'main branch-deploying new fitcher...' } }
    }
  
}
